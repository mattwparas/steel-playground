{
  "author": "Matthew Paras <mattwparas@gmail.com>",
  "name": "web-wasm",
  "version": "0.1.0",
  "scripts": {
    "build": "rimraf dist pkg && npm run build:wasm && npm run build:web",
    "build:web": "vite build --base './'",
    "build:wasm": "node etc/wasm-pack.mjs",
    "dev": "vite dev",
    "start": "vite dev",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@types/codemirror": "0.0.96",
    "@vitejs/plugin-vue2": "^2.3.3",
    "rimraf": "^3.0.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "buefy": "^0.8.20",
    "codemirror": "^5.61.0",
    "vue": "^2.6.12"
  }
}
